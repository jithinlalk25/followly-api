# Use the official Node.js image as the base image
FROM node:22-alpine

# Set the working directory
WORKDIR /app

# Copy files from your computer into the image
COPY . .

# Install the dependencies
RUN npm install

# Build the application (required for node dist/src/worker-email-main)
RUN npm run build

# Run the email worker (send-email BullMQ consumer, no HTTP server)
CMD ["npm", "run", "start:worker:email"]
